{
  "documentation": "Consolidate the accounting.",
  "fields": [
    {
      "documentation": "The name of the account set by the user.",
      "name": "name",
      "type": "String",
      "nullable": false
    },
    {
      "documentation": "The base to calculate the current amount of money. Values are stored in 100 cent.",
      "name": "initialValue",
      "type": "Integer",
      "nullable": false
    }
  ],
  "constraints": [
    {
      "name": "unique_name",
      "definition": "UNIQUE (account__name) ON CONFLICT FAIL"
    }
  ]
}